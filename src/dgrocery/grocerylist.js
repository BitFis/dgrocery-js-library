// @flow

/**
 * GroceryList represents a entry list of groceries
 */
export default class GroceryList {
    constructor() {
        // WIP
    }

    update(key: number, value: string) {
        console.log(`WIP: ${key}: ${value}`);
    }

    get(key: string): string {
        console.log(`WIP: ${key}`);
        return "";
    }

    toString() {
        return `{}`;
    }
}
